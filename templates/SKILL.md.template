---
name: skill-name-here
description: 一句話說明「什麼情境下要用這個技能」，包含具體觸發詞。範例：分析台股個股籌碼分佈，識別法人與大戶動向。當使用者提到「籌碼分析」、「大戶持股」、「法人買賣超」時使用。
# name 限制（官方 Agent Skills 規格）：kebab-case、最長 64 字元、不可開頭/結尾連字號
# description 限制（官方規格）：不可包含 < >、最長 1024 字元
#
# 以下為選用欄位：
# disable-model-invocation: true        # 只允許人類手動觸發時才加
# license: "..."                         # 官方欄位，選用
# allowed-tools: [...]                   # 官方欄位，選用，限制可用工具
# compatibility: "..."                   # 官方欄位，選用，說明相依的工具/環境
#
# 以下為本專案自訂擴充欄位（官方規格沒有，見 AGENTS.md 第 2 節）：
# type: "action"                         # 若會實際呼叫外部工具/API/發送訊息
# authorized_mcp_tools: []                # action 型技能列出授權工具白名單
# semantic_firewall: true                 # 是否啟用語意防火牆檢查
---

# 技能標題（人類可讀）

一段話說明這個技能的定位：它解決什麼問題、什麼時候該被呼叫、跟其他技能的邊界在哪裡。

## 使用時機

- 情境一：...
- 情境二：...

## 流程

### 1. 第一步

說明具體怎麼做。

### 2. 第二步

...

## 範例

給一個最小可行的輸入/輸出範例。

## 進一步細節

如果有大量參數表、完整 API schema、大量 edge case，不要塞在這裡，
移到同資料夾的 `REFERENCE.md`，這裡只寫一句連結：

參見 [REFERENCE.md](./REFERENCE.md) 了解完整參數與 edge case。

---

## Bundled Resources（選用子資料夾，官方三類）

- `scripts/`    — 決定性/重複性操作寫成腳本執行，不要每次靠 LLM 重算
- `references/` — 需要時才載入 context 的詳細文件（大檔案建議附目錄）
- `assets/`     — 輸出會直接用到的檔案（範本、圖示、字型等），不會被載入 context，
                   只在產出結果時被複製/引用
